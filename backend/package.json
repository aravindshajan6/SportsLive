{
  "name": "sportscast-backend",
  "version": "2.0.0",
  "description": "Sportscast API: live football scores (LiveScore via RapidAPI), RSS news aggregation, user comments",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "npm install && npm install --prefix ../frontend && npm run build --prefix ../frontend",
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=20.18.0"
  },
  "author": "Aravind Shajan",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.2.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.24.0",
    "morgan": "^1.10.1",
    "rss-parser": "^3.13.0",
    "zod": "^4.1.0"
  }
}
