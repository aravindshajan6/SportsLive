# Copy to `.env` (next to docker-compose.yml) and fill in the values.
RAPIDAPI_KEY=your-rapidapi-key-for-livescore-sports
JWT_SECRET=change-me-to-a-long-random-string
# Host port for the web app (the only port that is published)
FRONTEND_PORT=8080
